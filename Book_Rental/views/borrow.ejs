<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Borrow</title>
</head>
<body>
    <table>
        <tr>
            <th>Book Name</th>
            <th>Author</th>
            <th>Price/day</th>
            <th>Duration</th>
            <th>Compensation Price</th>
            <th>Contact Number</th>
            <th>Description</th>
            <th>Renter</th>
            <th>Borrow?</th>
        </tr>
        <% for(let i of books ){ %>
            <% if ( i.borrower == null && i.Validatedborrower == null) { %>
            <tr>
                <td><%= i.bookname %></td>
                <td><%= i.bookauthor %></td>
                <td><%= i.borrowprice %></td>
                <td><%= i.borrowduration %></td>
                <td><%= i.compensationprice %></td>
                <td><%= i.phno %></td>
                <td><%= i.description %></td>
                <td><%= i.renter.name %></td>
                <td><a href="/users/borrowal?rid=<%= i.renter.id %>&book=<%= i.id %>">Borrow</a></td>
            </tr>
            <% } %>
        <% } %>
    </table>
</body>
</html>